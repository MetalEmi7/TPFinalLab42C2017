import { ConfigMapDirective } from './config-map.directive';

describe('ConfigMapDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfigMapDirective();
    expect(directive).toBeTruthy();
  });
});
